## Before — Inbox productivity

Captured from the baseline Web worktree at commit `ec98a423b76492b85b31963837666b68230448f8` on 2026-08-22 after authenticating with the local test account. The baseline Inbox has the existing search/date/label/size controls, unread and attachment toggles, the traditional message list/reader split, and the existing folders. It does not render the v0.8 Smart Inbox sections/tabs or the Workspace productivity dashboard surfaces.

The baseline was served from a detached temporary worktree on port 5174 and is not part of the product branch. No real user data or external credentials were used.

## Before — Compose

Captured from the authenticated baseline Inbox on commit `ec98a423b76492b85b31963837666b68230448f8`. The baseline Compose dialog has To/Cc/Bcc, Subject, basic rich-text controls, AI write/rephrase/shorten, attachment, scheduling, undo delay, Save draft, Send, and Close. This capture is the baseline for comparing the v0.8 productivity-oriented flow; it uses only the local test account and local services.
